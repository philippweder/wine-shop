const config = {
  plugins: [
    "@tailwindcss/postcss",
    // autoprefixer is often implicitly handled by Next.js or @tailwindcss/postcss
    // but can be added explicitly if needed:
    // "autoprefixer",
  ],
};

export default config;
