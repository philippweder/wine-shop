# This file can be used for other Pydantic schemas if needed in the future.
# Wine-specific schemas have been moved to wine_app_schemas.py
# RAG-specific schemas are in the schemas/rag_schemas.py

from pydantic import BaseModel

# Example of a non-wine, non-RAG schema if you had one:
# class OtherSchema(BaseModel):
#     field: str
